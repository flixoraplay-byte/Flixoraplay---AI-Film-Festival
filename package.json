{
  "name": "flixoraplay",
  "version": "1.0.0",
  "description": "FlixoraPlay - Premium AI-Generated Video Competition Platform",
  "scripts": {
    "dev": "wrangler pages dev . --compatibility-date=2024-09-23",
    "db:init": "wrangler d1 execute flixoraplay-db --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute flixoraplay-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.96.0"
  }
}
